# CHANGELOG

## v2.28
- 西松屋 `/04/`・`/05/`・`/06/` を最下部までスクロールして全遅延画像を読み込む処理を追加。
- 各ページの大きな商品画像を商品ブロック単位で個別保存。
- 保存した各ブロックを個別OCRし、商品名・価格・ブロック画像を商品カードへ紐付け。
- 商品ブロック取得上限を3ページ合計240件へ拡張。
- 個別ブロック検出失敗時のみ従来の縦分割スクリーンショットへフォールバック。

## v2.27
- `collectDomProducts is not defined` を修正。
- DOM商品情報取得の共通関数を `scraper.js` に復元。
- 日本語Webフォントの読み込み待機処理を共通関数として復元し、スクリーンショット前に実行。
- 西松屋 `/04/`・`/05/`・`/06/` 方式はV2.26のまま維持。
- 店舗スキップ時に前回表示を維持する仕様も継続。
- READMEは同じバージョンの変更点を複数見出しにせず、1つの見出し下の箇条書きへ統一。
- 詳細な更新履歴は `CHANGELOG.md` に集約。

## v2.26
- 西松屋は今週の `period-...` URL 確定後、`/04/`・`/05/`・`/06/` の3ページを直接読み込む。
- 「衛生・調乳グッズ」という文字やリンクを探す処理は使用しない。
- 西松屋については上記3URL以外のカテゴリリンク探索を行わない。
- 今週URLが確定できない場合はURLを推測しない。
- その他のV2.25仕様は維持。

## v2.25
- v2.24 の「スキップ時は前回表示を維持」を継続。
- 西松屋デジタルチラシ入口で、今週の `period-...` URL が確定するまで最大15秒待機。
- リダイレクト完了を確認してから `/04/` を追加するようにし、読み込みタイミングによる取得失敗を抑制。
- `period-...` URL が確認できない場合は従来どおり推測せず処理を止める。
- `/04/` ページの商品は引き続き掲載商品すべてを対象にし、DOM＋OCR＋商品画像で解析。

## v2.24
- 店舗をスキップした場合、その店舗の保存済み前回表示をそのまま維持。
- スキップの空結果をDBへ保存しないよう変更し、フロント側でも前回表示を上書きしない二重保護を追加。
- 西松屋の入口を `https://sale.24028.jp/digital-chirashi/` に変更。
- 入口アクセス後にリダイレクトされた今週の `period-...` URLを取得し、その末尾へ `/04/` を追加して直接解析。
- period URLを確認できない場合は推測でURLを作らず、確認不足として扱う。
- `/04/` ページは掲載商品すべてを対象としてDOM取得＋OCRを継続。

## v2.23
- Vercel Chromiumで日本語が描画されないケースに対応。
- スクリーンショット前に日本語Webフォント（Noto Sans JP）を実行時に読み込み、`document.fonts.ready` を待機。
- 日本語フォント取得がCSPや通信で失敗しても商品を落とさないよう、HTML(DOM)から商品名・価格を直接取得。
- DOMの商品名・価格・商品画像URLをOCR結果と統合。
- 西松屋「衛生・調乳グッズ」はDOMの商品情報を優先しつつ、商品ブロックOCRと全体OCRを併用。
- その他の店舗でもスクリーンショット型チラシはDOM商品情報を利用できる場合に併用。
- 遅延読み込み対策（自動スクロール）はv2.22のまま全店舗に適用。
- フォントファイル自体はリポジトリへ同梱せず、実行時Webフォント方式。

## v2.22
- 西松屋だけでなく、スクリーンショットを使う全店舗に遅延読み込み対策を適用。
- `loading="lazy"` の画像を可能な範囲で eager 化し、`data-src` / `data-lazy-src` / `data-original` 等も補完。
- ページ上部から最下部まで段階的に自動スクロールし、各位置で画像の読み込み完了を待機。
- 未読み込みの大きな画像が残る場合は、その画像位置へ追加スクロールして再待機。
- 最下部到達後に上へ戻して追加待機してからチラシを撮影。
- 分割スクリーンショットでも各区間の撮影前に画像表示を待つよう変更。
- 西松屋、アカチャンホンポ、バースデイ、ダイレックス、ドラッグストアモリ、コスモス、レデイ薬局、クスリのアオキ、ドンキ等の共通撮影処理に適用。

## v2.21
- 西松屋はデジタルチラシ「衛生・調乳グッズ」をクリックし、遷移先の掲載商品をすべて対象としてOCRします。
- 遷移先URLは固定せず、毎回リンク先を取得します。
- 西松屋の対象ページではカテゴリ判定に一致しなくても、商品名と価格を読めた商品を表示します。
- 縦長ページは最大18分割で上から下まで精査します。
- 商品ブロック画像を商品カードのサムネイルとして利用します。
- 既存のバージョン別NOTESファイルを廃止し、更新履歴をCHANGELOG.mdへ統合しました。

# V2.9 changes

- タイトルを `ver 2.9` に更新。
- 更新中の進捗をNeon経由で約1.2秒ごとに表示。
  - 開始
  - 店舗ページ確認中
  - チラシを検索中
  - チラシを発見
  - チラシをダウンロード中
  - スクリーンショット取得中
  - チラシ保存中
  - OCRを実行中
  - 日付確認中
  - 商品抽出中
  - 完了 / エラー
- コストコオンラインを対象に再追加。
- コストコオンラインは公式のベビー・キッズカテゴリを直接解析し、対象カテゴリかつ `○○円引き後`（または同等の `○○円OFF` 表記）が明記された商品だけ表示。
- コストコはチラシOCRではなく公式HTMLをUTF-8/charsetに従ってデコードし、文字化けを抑制。
- コストコ商品のカードに `○○円引き後` を表示。
- 通常店舗の2分 / 文字量が多い場合最大5分、2段階最新性チェックは継続。

# V2.8 変更点

- 「特価・SALE・値引き・クーポン」などの販促ワード必須条件を廃止。
- 最新性チェックを通過したチラシ内に掲載されている対象カテゴリの商品をすべて抽出対象に変更。
- 対象カテゴリは以下の5つ。
  - おむつ・おしりふき
  - 粉ミルク・液体ミルク
  - 離乳食・ベビーフード
  - おもちゃ
  - ベビーケア・その他
- ベビー服・ロンパース・肌着・スタイ等は引き続き除外。
- 価格がOCRできない商品も除外せず、価格を「不明」として表示。
- チラシレイアウトで商品名と価格が離れている場合に備え、前後3行を同一商品候補として解析。
- 画面タイトルを「ベビー用品 チラシチェッカー」に変更。
- 表示バージョンを ver 2.8 に更新。
- V2.7の2段階最新性確認、スクリーンショットOCR、2分/最大5分モードはそのまま維持。

# V2.7変更点

- 通常の1店舗解析目安を120秒（2分）に設定。
- PDF、複数チラシ、大容量ファイル、またはOCR文字量が多い場合のみ最大285秒（UI上は最大300秒/5分）まで自動延長。
- ブラウザ側のリクエスト上限を300秒に変更。
- 画像/PDFの直接取得に失敗した場合、チラシ表示領域（img/canvas/iframe/flyer系コンテナ）のみスクリーンショットしてOCRするフォールバックを追加。
- ページ全体のスクリーンショットは使用しない。
- 最新性チェックを2段階化。
  1. 公式サイト/Shufoo!など掲載側の売出期間・日付を確認。
  2. 実際のチラシ画像/PDF/スクリーンショット内の日付をOCRして確認。
- 2段階の日付が近い範囲で一致した場合のみ「2段階一致」と表示。
- 片方が不明、または日付が矛盾する場合は「最新」と断定しない。
- 明確に古い、または日付不一致のチラシは商品抽出対象から除外。
- PDF OCR上限を最大6ページへ拡張。
- タイトル表示を ver 2.7 に更新。

# V2.6

- 1店舗のブラウザ側タイムアウトを55秒から120秒へ変更。
- サーバー側は112秒をソフト上限にし、上限接近時は残りOCRを省略して次へ進む。
- チラシ画像/PDF自体をOCRし、売出期間・日付を解析。
- `現在有効 / 最近終了 / 近日開始 / 日付不明 / 古い可能性` を判定。
- 古いと判定できたチラシは商品抽出対象から除外。
- 日付を読めない場合は「最新」と推測せず「日付不明」。
- PDF.js workerをVercel Functionへ明示同梱し、workerSrcも実ファイルへ指定。
- PDF OCR上限を4ページへ拡張。
- 画面のチラシリンクに最新性ラベルを表示。
- タイトル横を `ver 2.6` に更新。

# V2.5
- 対象をユーザー指定の9店舗に固定。
- 公式店舗ページを最優先、取得できない場合のみ同一店舗のShufoo!へフォールバック。
- 店舗名・住所をページ本文で照合し、一致しないページの画像は採用しない。
- 通常ページ全体のスクリーンショットを「チラシ」としてOCRする処理を廃止。
- チラシらしい画像/PDFだけを保存・OCR。
- 取得元（公式/Shufoo!）を画面に表示。
- ver 2.5 表示。

# V2.4

- ベビー服を抽出対象から除外
- カテゴリを5分類に再編
- カテゴリごとの表示/非表示切替を追加
- 店舗ごとの表示/非表示切替を追加
- FAQ・カテゴリ一覧・一般説明文の誤抽出を抑制
- 特価/Sale/値下げ/OFF/クーポン/期間限定/目玉/広告の品/お買得等の販促シグナルがあるものだけ抽出
- 価格または割引額/率を確認できない候補は表示しない
- タイトル横を ver 2.4 に更新

# V2.3 修正内容

- Vercel Function に `tesseract.js-core/**` を明示的に同梱。
- OCR 初期化時に `node_modules/tesseract.js-core` を `corePath` として明示。
- `tesseract-core-simd.wasm` 欠落による ENOENT 対策。
- Chromium / Canvas のランタイムアセットも引き続き明示同梱。
- 店舗ページ取得・画像/PDFダウンロード・OCR・PDF描画にタイムアウトを追加。
- 1店舗の処理が長引いた場合、一部OCRを省略してリンク/WEB本文結果を残す。
- ブラウザ側では1店舗55秒で次店舗へ進む。
- 更新中に店舗名、件数、経過秒数を表示。
- 各店舗に前回処理時間と警告を表示。
- 1店舗の失敗で全10店舗更新を中断しない。
- タイトル横のバージョン表示を `ver 2.3` に更新。

# V2.20

- 西松屋は `https://www.24028.jp/digital-chirashi/` を起点に変更。
- ページ内の「衛生・調乳グッズ」を文字で特定してクリックし、遷移先の画像を十分待って取得・OCR。
- チラシのスクリーンショットは全体OCRに加え、重なりのある商品ブロックへ分割してOCR。
- 商品ブロックから抽出できた商品は、そのブロック画像を商品カードのサムネイルとして保存・表示。
- アカチャンホンポ「紙おむつセール」は対象ページの商品をブランド名でも判定し、商品ブロック単位で表示。
- アカチャンホンポ「アカトク」は従来どおり縦長ページ全体を精査し、ミルク・離乳食を抽出。
- バースデイ、ダイレックス、ドラッグストアモリ、コスモス、レデイ薬局、クスリのアオキ、ドンキも、スクリーンショット取得できたチラシでは商品ブロックOCRを併用。
- 全体OCRは小さい文字を補うフォールバックとして残す。
- 読めない商品名・価格は推測しない。

# V2.19 Notes

- アカチャンホンポ ゆめタウン徳島店は `https://stores.akachan.jp/339#deals` の表示文字を直接確認します。
- バナー画像OCRによる対象判定は廃止しました。
- `紙おむつセール` を最大2件、`アカトク` を最大2件、DOM上の文字で個別に特定して実クリックします。
- アカトクはクリック先の縦長ページを上から下まで精査し、必要に応じて最大18分割でスクリーンショットします。
- アカトクからは「粉ミルク・液体ミルク」「離乳食・ベビーフード」のみ抽出します。
- 紙おむつセールからは「おむつ・おしりふき」のみ抽出します。
- アカチャンホンポの商品カードにはOCR元のスクリーンショット画像を小さく表示し、`アカトク1` など取得元ラベルを表示します。
- 見つからないバナーを推測でクリックしません。
- V2.18.1までのNeon互換、Private Blob、前回表示維持、手動キャッシュ削除、コストコ重複排除などは維持します。

# V2.18.1

- V2.18 のブランド・メーカー・シリーズ名によるカテゴリ判定を維持。
- Neon/Vercel Storage の既存環境変数との互換性を強化。
- `DATABASE_URL` だけでなく `POSTGRES_URL`, `DATABASE_URL_UNPOOLED`, `POSTGRES_URL_NON_POOLING`, `POSTGRES_PRISMA_URL`, `POSTGRES_URL_NO_SSL` を順に自動判定。
- URL形式の変数がない場合は `PGHOST` / `PGUSER` / `PGPASSWORD` / `PGDATABASE` からSSL接続URLを構築。
- 接続情報やパスワードはUI/ログへ表示しない。
- V2.17までのBlob表示修正、前回表示維持、手動キャッシュ削除、UI、コストコ重複排除を維持。

# V2.18 更新内容

- おむつ・ミルク・離乳食をカテゴリ名だけでなくブランド名・シリーズ名からも判定。
- OCRの全角/半角、英字大小文字、空白・中黒などを正規化して照合。
- パンパース、メリーズ、ムーニー、グーン、マミーポコ等をおむつとして判定。
- ほほえみ、はぐくみ、ぴゅあ、すこやか、アイクレオ、はいはい等をミルクとして判定。
- キユーピー、和光堂系シリーズ、ピジョン系シリーズ等を離乳食・ベビーフードとして判定。
- 商品データのnotesに実際に反応した判別語を保存。
- V2.17のPrivate Blob修正および既存機能を維持。

# V2.17 更新内容

- Private Vercel Blob の表示エラー `Vercel Blob: missing options, see usage` を修正。
- `/api/flyer` で `get(url, { access: 'private' })` を使用。
- BlobのContent-Typeを保持し、PDF/PNG/JPEG等をブラウザ内で表示。
- `Cache-Control: private, no-store` と `Content-Disposition: inline` を設定。
- V2.16の通常更新時の前回表示維持、手動キャッシュ削除、UI、コストコ重複排除を維持。

# V2.16 更新内容

- 通常の「最新情報に更新」ではキャッシュ/Blobを削除しないよう変更。
- 前回取得済みの店舗・商品・チラシを表示したまま、新しい取得が成功した店舗だけ順次差し替える方式に変更。
- 上部に「キャッシュ削除」ボタンを追加。希望時のみ現在表示DBキャッシュと保存済みチラシBlobを削除。CSV履歴は保持。
- レイアウトを再整理し、店舗・カテゴリ・進捗・ステータスにアイコンを追加。
- 最新性、処理時間、取得元をチップ表示にして視認性を改善。
- コストコオンラインの商品カードを見やすくし、小さめの商品画像表示を継続。
- コストコオンラインの重複商品を、商品URL → 商品番号 → 正規化商品名の順で同一判定し1件に統合。
- コストコオンラインは引き続き「抽出方法」を非表示。
- V2.15までのアカチャンホンポ/レデイ薬局専用取得、公式URL固定、Shufoo!不使用、店舗スキップ、通常2分/最大5分を維持。

# V2.15

- アカチャンホンポは https://stores.akachan.jp/339#deals を起点に固定。
- セール・チラシ情報内のバナー画像をOCRし、「アカトク」「紙おむつSALE/紙おむつセール」を特定してからクリック先を撮影・OCR。
- レデイ薬局は指定公式ページ上の対象を実クリックし、新規タブ/遷移先をそのまま待機・撮影する方式へ変更。
- 更新完了メッセージにスキップ店舗名・失敗店舗名を表示。
- V2.14までのコストコ商品画像表示・抽出方法非表示、公式URL固定、Shufoo非使用、キャッシュクリアを継続。

# V2.14 changes

- バースデイ藍住店は「拡大して見る」のリンク先URLを直接開かず、店舗ページ上のボタンを実際にクリックし、開いたタブ/遷移先をそのまま撮影する方式に変更。
- Access Denied / Forbidden を検出した画像は保存しない。
- 「チラシ1/2」リンクは、実際に表示可能なBlob viewer URLまたはHTTP URLがあるものだけ表示。
- Private Vercel Blobの読み出しAPIを現行 `get(url)` 方式に修正し、ストリーム取得を堅牢化。
- コストコオンラインの赤文字「引き後」商品に公式商品画像URLを取得できた場合、小さめのサムネイルを表示。
- コストコオンラインの商品カードでは「抽出方法」を非表示。
- 既存のShufoo!無効、公式URL固定、スキップ、キャッシュ削除、通常2分/最大5分などは継続。

# V2.13 changes

- Shufoo! fallback/search removed from every store. Only the user-specified official URLs are configured.
- Manual refresh first deletes old `flyers/` objects from Vercel Blob and clears current store/progress cache in Neon. Historical CSV rows remain.
- Vercel Blob proxy is now `no-store` to avoid stale browser caching.
- Browser cache is disabled and cleared before acquisition.
- Screenshots wait longer: after navigation/click the app waits for document completion, large images to finish loading (`complete`/`naturalWidth`), or a rendered canvas, then waits an additional moment before capture.
- The progress panel has a `この店舗をスキップ` button. It aborts the client request and also writes a server-side skip request so the running worker exits at the next progress checkpoint.
- Costco Online exposes two information-source buttons, one for each user-specified Costco URL.
- Costco Online scans only those two URLs and displays product tiles containing a red rendered `引き後` label. Category keyword filtering is not used for Costco extraction.
- Version label: ver 2.13.

# V2.12 NOTES

- アカチャンホンポ等で `Protocol error (Page.captureScreenshot): Unable to capture screenshot` が出ても店舗処理全体を失敗させないよう修正。
- チラシ取得を Canvas直接PNG化 → 大画像の直接DL → チラシ要素スクリーンショット → 固定ビューポート分割スクリーンショットの順にフォールバック。
- 巨大なfullPage screenshotは使用せず、最大約1050px単位に分割してOCRする。
- スクリーンショット1枚の失敗は無視して次の分割領域を試す。
- コストコオンラインは指定2URL内で、ブラウザのcomputed style上「赤文字」と判定され、かつ「引き後」を含む表示がある商品を抽出。
- コストコでは商品カテゴリによる除外は行わない（表示UI上のカテゴリ割当のみURL別に維持）。
- タイトル表示を `ver 2.12` に更新。

# V2.11 NOTES

- 西松屋 徳島南矢三店の公式ページに「現在、セール情報はありません。」がある場合は、チラシ探索・スクリーンショット・OCRを行わず正常スキップ。
- コストコオンラインは指定済み2URLのみ対象。
- コストコは商品カテゴリ/ベビー用品キーワードによる絞り込みを行わず、ページ内で literal `¥○○引き後` / `￥○○引き後` が確認できる商品をすべて表示。
- コストコ商品の表示カテゴリはURL由来で割り当てるため、既存の表示/非表示UIから消えない。
- `Access Denied` / `Forbidden` 等のブロック画面はチラシスクリーンショットとして保存しない。
- タイトルを `ver 2.11` に更新。

# V2.10 NOTES

- 店舗別にユーザー指定URL/クリック手順を優先する `storeSpecificFlow` を追加。
- クリック先はチラシ表示領域を優先してスクリーンショット。縦長/複数ページは分割撮影。
- 「次のチラシ」「チラシをめくる」「裏面」「次へ」などを店舗別に追跡。
- Donki URLを `shop_id=576` に修正。
- くすりのレデイの一次情報元を `https://shop.tsuruha-g.com/4375` に変更。
- コストコ対象URLを cos_8.4 / cos_8.5 の2ページに固定。
- コストコ抽出条件を literal `¥○○引き後` / `￥○○引き後` に限定。
- UIの「対象」は店舗名のみ。
- タイトル表示を `ver 2.10` に更新。

