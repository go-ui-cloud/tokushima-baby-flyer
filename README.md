# 徳島 ベビー用品チラシチェッカー ver 2.30.8

GitHub → Vercel 配備用のNext.jsアプリです。指定された各社の公式URLのみを使い、チラシ/公式オンライン商品を取得・OCR・抽出します。Shufoo!検索は使用しません。

## V2.30.8の変更
- バースデイ藍住店は指定の公式店舗ページ `map_detail_3510.html` の「拡大して見る」を実クリック。
- 拡大先のチラシを下まで読み込んでからスクリーンショット処理。
- チラシを商品単位のブロックへ分割し、各ブロックを個別にOCR。
- OCR結果を既存のベビー用品カテゴリ判定へ渡し、商品名・価格・カテゴリ・画像を表示。
- 個別ブロック検出に失敗した場合だけ、縦分割スクリーンショットOCRへフォールバック。
- Shufoo検索は使用しない既存方針を維持。

## V2.30.7の変更
- 西松屋は「1読み取り画像 = 1商品カード」に変更し、57枚取得した場合は原則57件を別々に保持。
- すでに商品単位で切り出した西松屋画像を、OCR時にさらに分割する処理を停止。
- 西松屋の重複除去を商品名・価格ベースから画像URLベースへ変更し、「商品名不明／価格不明」が複数あっても1件に潰れないよう修正。
- 西松屋専用の価格抽出を追加し、100円未満の数字を価格候補から除外。
- `¥1,399` や `¥1,538税込` のような円記号・税込表記を優先して価格を決定。
- 分類できない商品は引き続き「その他」、OCRできない項目は「不明」で画像を残す。
- アカチャンホンポの価格付き商品ブロック表示などV2.30.6までの仕様を維持。

## V2.30.6の変更
- 西松屋でOCR済みの商品ブロックが最終表示時に消える不具合を修正。
- 西松屋は代表チラシURLによる商品フィルターを行わず、/04/・/05/・/06/ から取得した商品ブロックを表示対象にする。
- 西松屋の商品ブロックは最大240件まで保持し、カテゴリ判定不能の商品は従来どおり「その他」で表示。
- V2.30.5のアカチャンホンポ「価格が確認できる商品ブロックのみ表示」仕様は維持。

## V2.30.5の変更
- アカチャンホンポも取得画像を商品ブロック単位に分割してOCRする方式へ統一。
- アカチャンホンポの商品表示は、商品ブロック内で価格を確認できたものだけに限定。
- 表示商品カードには実際にOCRした商品ブロック画像を紐付ける。
- アカチャンホンポでは全体OCRやDOM抽出だけで商品カードを作らず、商品ブロックOCRを表示根拠にする。
- 「紙おむつセール」「アカトク」の取得方法は従来どおり維持。
- 西松屋の全ブロック表示、「その他」カテゴリ、パンツタイプ判定、V2.30.1由来のDB接続/継続処理は継続。

## 対象

- 西松屋 徳島南矢三店
- バースデイ 藍住店
- アカチャンホンポ ゆめタウン徳島店
- ダイレックス 田宮店
- ドラッグストアモリ 徳島住吉店
- ドラッグストアコスモス 住吉店
- くすりのレデイ 田宮街道店
- クスリのアオキ 北島田店
- MEGAドン・キホーテ徳島店
- コストコオンライン

各店舗はこれまで指定された公式店舗URLとクリック手順を使用します。アカチャンホンポは `https://stores.akachan.jp/339#deals` の表示文字から「アカトク」「紙おむつセール」を特定してクリックします。西松屋は「現在、セール情報はありません。」と表示される場合、OCRを行わずスキップします。

## コストコオンライン

下記2ページだけを対象にします。

- `https://www.costco.co.jp/Baby-Kids-Toys/Diapers-Wipes/c/cos_8.4`
- `https://www.costco.co.jp/Baby-Kids-Toys/Formula-Kids-Snacks/c/cos_8.5`

赤文字で「引き後」が確認できる商品だけを表示します。カテゴリによる除外は行いません。商品画像URLが取得できる場合は小さめの画像を表示します。

## OCR / 更新時間

- 通常は1店舗300秒（5分）。
- PDF・複数ページ・文字量が多い場合は必要な場合は5分処理を最大2回に分割し、合計約10分まで継続。
- 進捗は「店舗ページ確認中」「チラシを発見」「OCRを実行中」「日付確認中」「商品抽出中」などをリアルタイム表示。
- 進捗欄の「この店舗をスキップ」で現在店舗を飛ばせます。
- 最新性は、掲載元ページの日付 + チラシ画像/スクリーンショット内の日付の2段階で確認します。
- 日付不明や不一致を推測で最新扱いしません。

## 更新とキャッシュ

「最新情報に更新」では前回表示を維持します。店舗の新しい取得が成功した場合のみ、その店舗結果を差し替えます。失敗/手動スキップ時は前回結果が画面に残ります。

「キャッシュ削除」は明示的に押したときだけ実行されます。現在表示用DBキャッシュと `flyers/` Blobを削除しますが、更新履歴/CSV履歴は保持します。

## Vercel連携

- Neon Postgres: `DATABASE_URL`（V2.19では `POSTGRES_URL` / `DATABASE_URL_UNPOOLED` / `POSTGRES_URL_NON_POOLING` / `POSTGRES_PRISMA_URL` / `PG*` も自動認識）
- Vercel Blob: 接続済みPrivate Blob（OIDC / `BLOB_STORE_ID`）

既存のNeon/Blob設定をそのまま利用できます。

## デプロイ

GitHubリポジトリの中身をV2.30.5で上書きしてCommit/Pushしてください。VercelのGit連携が有効なら自動再デプロイされます。

## V2.31.0
- 西松屋の商品抽出を再構築し、「1読み取り画像 = 1商品カード」に固定。
- 各画像を他画像と混ぜず個別OCRし、その画像だけから商品名・価格・カテゴリを決定。
- OCRで商品名や価格を読めなくても画像カードは必ず残す。
- 分類できない商品は「その他」へ。
- 100円未満の誤読価格は採用しない。
- 円記号・税込表記を優先して価格を選ぶ。
- 西松屋の重複除去は画像URL基準。

## V2.31.1
- 西松屋の商品名OCRを商品画像の左下領域に限定。
- 西松屋の価格OCRを商品画像の右下領域に限定。
- 価格は赤い本体価格ではなく、その下の「税込」価格のみ採用。
- 右下から税込価格を取得できない画像は商品一覧に表示しない。
- OCR精度向上のため商品名領域を2.8倍、価格領域を3.2倍に拡大して認識。
