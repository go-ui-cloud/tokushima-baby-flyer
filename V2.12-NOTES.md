# V2.12 NOTES

- アカチャンホンポ等で `Protocol error (Page.captureScreenshot): Unable to capture screenshot` が出ても店舗処理全体を失敗させないよう修正。
- チラシ取得を Canvas直接PNG化 → 大画像の直接DL → チラシ要素スクリーンショット → 固定ビューポート分割スクリーンショットの順にフォールバック。
- 巨大なfullPage screenshotは使用せず、最大約1050px単位に分割してOCRする。
- スクリーンショット1枚の失敗は無視して次の分割領域を試す。
- コストコオンラインは指定2URL内で、ブラウザのcomputed style上「赤文字」と判定され、かつ「引き後」を含む表示がある商品を抽出。
- コストコでは商品カテゴリによる除外は行わない（表示UI上のカテゴリ割当のみURL別に維持）。
- タイトル表示を `ver 2.12` に更新。
