# V2.2

- 画面タイトル横に `ver 2.2` を表示。
- Vercel `/api/update` に `@sparticuz/chromium/bin/**` と `build/**` を明示的にトレース。
- Chromium の bin ディレクトリを明示して `executablePath()` を解決。
- Chromium 起動不能時は HTTP + Cheerio でチラシ画像/PDF探索を継続。
- 本番ビルドは `next build --webpack` を使用。
- Neon / Private Vercel Blob(OIDC) の V2.1 構成は維持。
